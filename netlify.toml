[build.environment]
HUGO_VERSION = "0.140.0"
TZ = "Asia/Kolkata"

[build]
publish = "public"
command = "hugo --gc --minify --config hugo.toml"

# Redirects are managed in static/_redirects file
# Run `node scripts/generate-redirects.js` to regenerate from content
